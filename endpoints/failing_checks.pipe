DESCRIPTION >
    Retrieve checks which its last result was not successful in the last 30 minutes

TOKEN "endpoint_unscoped" READ

NODE failing_checks_for_active_checks
SQL >
    %
    SELECT * EXCEPT success
    FROM (
        SELECT
            check_name AS check,
            argMax(scope, timestamp) AS scope,
            argMax(toString(timestamp,  {{ String(tz, 'Europe/Madrid') }}), timestamp) AS time,
            argMax(success, timestamp) AS success,
            argMax(http_status, timestamp) AS http_status,
            argMax(duration_seconds, timestamp) AS duration_seconds,
            argMax(error, timestamp) AS error
        FROM results
        WHERE timestamp >= now() - INTERVAL 30 MINUTE
        GROUP BY check_name
    )
    WHERE success = false
    ORDER BY check

TYPE ENDPOINT
